# Task State

## Objective

Reduce the orchestrator's always-injected baseline by >=30% while preserving every required behavioral rule.

## Acceptance Criteria

1. Always-injected instruction baseline = `AGENTS.md` + `.opencode/agents/orchestrator.md` (the content injected on every orchestrator turn; tool schemas ~2.6K tokens are out of scope as not reasonably reducible). Baseline measured 2026-09-21: AGENTS.md 17315 chars, orchestrator.md 11302 chars, combined 28617 chars.
2. Post-change combined size <= 20032 chars (>=30% reduction). Exact before/after char counts reported for every changed file.
3. Every behavioral rule in the baseline remains present and enforceable; no rule deleted, weakened, or made unreachable. A rule mapping (old location -> new location / retained-as-is) must be produced.
4. Safety, security, scope-control, validation, and agent-authority/ownership rules remain always-injected. Any rule relocated to an on-demand artifact (e.g. a skill) must be justified and must not make an always-required rule unreachable.
5. Only reduction-target files modified. Pre-existing unrelated working-tree changes (`architect.md`, `developer.md`, `reviewer.md`, `opencode.json`, `.task.md`) must not be reset, reverted, or overwritten.
6. Valid configuration: any new skill has valid frontmatter (name matches folder, description present); agent frontmatter intact; cross-references resolve.
7. Config/prompt-only validation: char counts, file integrity, frontmatter and cross-reference checks. No product test suite. Do not commit.

## Approved Decisions and Constraints

- Baseline (~10.5K tokens) split: AGENTS.md ~4.6K (44%), orchestrator.md ~3.0K (28%), tool schemas ~2.6K (25%).
- Prior completed task already rewrote these files and reduced combined size ~5.3%; this task must reach >=30%.
- Working tree already has unrelated modifications (architect.md, developer.md, reviewer.md, opencode.json, .task.md); preserve them.
- Prompt/configuration work only; run no broad tests; do not commit.

## Relevant Files

- `AGENTS.md` (final: 17315 → 11399 chars)
- `.opencode/agents/orchestrator.md` (final: 11302 → 7481 chars; frontmatter frozen)
- `.opencode/skills/bounded-discovery/SKILL.md` (new on-demand skill, 4287 chars; description 303 chars hosts relocated procedural detail)
- Reference only (do not modify): `.opencode/agents/architect.md`, `.opencode/agents/developer.md`, `.opencode/agents/reviewer.md`, `opencode.json`

## Architecture / Implementation Plan

- Mechanism: hybrid — in-place dedup/compression plus one on-demand skill. Mandatory always-injected rules (security, scope, validation, ownership/authority, routing, correction limit, completion) stay in AGENTS.md/orchestrator.md; only procedural discovery/tool-efficiency detail moves to the skill.
- AGENTS.md transforms: §1–§14 compress and remove duplicated prose (retain priority order, security mandates, validation matrix + 2-cycle limit, milestone role duties); merge §5 into §12; §15/§16 keep hard prohibitions and Completion/Scope Control/Context-Proportionality summary + pointer, relocate detailed Tool Efficiency / Exploration / Discovery lists to skill; §17 compress.
- orchestrator.md transforms: keep frontmatter byte-identical; §2 routing and §4 ownership keep exact mappings/allow-list; merge §5–§7 handoff templates; §8 keep 2-cycle limit; collapse §9–§13 into one Cross-cutting rules section with AGENTS.md pointers; §14 keep subagent allow-list; §15 compress.
- Skill `.opencode/skills/bounded-discovery/SKILL.md`: frontmatter `name: bounded-discovery` (matches folder) + trigger-keyword `description`; body = relocated Source/Discovery, Exploration, Tool Efficiency, Tool Failure, and Bounded-Discovery detail.
- Char budget: AGENTS ~9800 + orchestrator ~6300 + skill description ~300 ≈ 16400, leaving ≥3600 margin under 20032.
- Rule preservation: per-rule mapping (old location → retained/merged/compressed/relocated) produced during implementation; mandatory-phrase grep verifies always-injected rules remain.
- Validation: raw .Length per file and combined sum ≤20032; frontmatter diff for orchestrator; skill name/folder/description check; cross-reference grep; git diff scope check; no tests; no commit.

## Implementation Status

- Implemented 2026-09-21. Compressed `AGENTS.md` (17315 -> 11399) and
  `.opencode/agents/orchestrator.md` (11302 -> 7481); combined baseline 28617 ->
  18880 (-34.03%, margin 1152 under the 20032 target). Created
  `.opencode/skills/bounded-discovery/SKILL.md` (name matches folder; description
  303 chars) hosting the relocated discovery/tool procedural detail.
- Rule preservation: all mandatory rules remain always-injected (security, scope,
  validation with the single canonical 2-cycle limit in AGENTS.md section 10, git
  discipline, product-vs-technical, conflict resolution, architecture discipline,
  code quality, completion). AGENTS.md section 5 merged into section 12;
  orchestrator sections 9-13 collapsed into section 7 Cross-cutting Rules; single
  subagent allow-list in orchestrator section 8.
- Validation: char counts; orchestrator frontmatter byte-identical; skill
  frontmatter valid; cross-references resolve; git scope limited to the target
  files (pre-existing unrelated working-tree changes untouched). Not committed; no
  product test suite run.

## Review Findings

- All acceptance criteria pass; no actionable defects found.
- Rule preservation verified against the HEAD baseline (AGENTS.md 17336,
  orchestrator.md 10703) plus the implementation mapping, because the exact
  pre-change working-tree content (17315/11302) is unrecoverable (uncommitted;
  no stash, dangling blob, or snapshot found). Current orchestrator retains a
  1:1 correspondence with the baseline section structure; AGENTS.md section 5
  is merged into section 12 with no dangling section-5 references.
- Safety/security/scope/validation/agent-authority rules remained
  always-injected; only non-safety discovery/tool procedural detail moved to the
  skill.
- Minor non-blocking observation: `docs/OPENCODE_SETUP.md` (pre-existing, out of
  scope, not always-injected) restates the 2-cycle limit; within the changed
  baseline there is exactly one canonical statement (AGENTS.md section 10) and
  one subagent allow-list (orchestrator section 8).
- The skill could not be observed loading in the current process because opencode
  does not hot-reload config; validity confirmed structurally (path,
  name == folder, description present).

## Validation

- Measured char counts: AGENTS.md 11399, orchestrator.md 7481 (combined 18880);
  skill description 303 -> combined including description 19183 <= 20032 vs
  baseline 28617 (-32.97%; -34.03% excluding description).
- Orchestrator YAML frontmatter content identical to HEAD (first 19 lines);
  skill frontmatter `name: bounded-discovery` matches folder and `description`
  present and non-empty (303 chars).
- Section cross-reference grep: all targets resolve (AGENTS sections 8, 10, 15,
  16, 3-4, 11; orchestrator sections 2, 3, 6); no reference to removed AGENTS
  section 5.
- Scope: `git status --short` / `git diff --stat` show only AGENTS.md,
  orchestrator.md, and the new `.opencode/skills/bounded-discovery/SKILL.md`
  changed by this task; architect.md/developer.md/reviewer.md/opencode.json/
  .task.md diffs are pre-existing and predate the task's edit window (mtimes
  13:31-15:44 vs 17:22-17:56).
- Canonical-rule grep: single 2-cycle statement (AGENTS section 10); single
  subagent allow-list (orchestrator section 8); security non-exposure (AGENTS
  section 7) and confirmation-before-deletion (AGENTS section 11, orchestrator
  section 7) retained.
- No product test suite run (docs/config-only per scope); changes not committed.