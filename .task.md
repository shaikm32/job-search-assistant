# Task State

## Objective

Refine the Reviewer's validation strategy (and matching AGENTS.md guidance) so validation is scope-, risk-, and change-aware: the minimum sufficient validation for reasonable confidence, with full-suite execution allowed when justified but never the default. Adaptive agent routing, permissions, role boundaries, and the absence of a Tester/QA agent remain unchanged. No product code changes.

## Acceptance Criteria

1. `reviewer.md` defines a validation-selection strategy: before testing, identify changed files/components, affected behavior, existing tests/checks covering it, and the smallest sufficient validation set; then run it and explain why it was sufficient.
2. Validation scales with risk: documentation-only, UI-only, isolated backend, AI/provider, database/schema, cross-module, and high-risk/architectural changes each map to appropriate validation.
3. Running the full test suite is permitted when justified but is not the default.
4. Explicit statement that "minimum sufficient validation" does not mean "run fewer tests regardless of risk"; correctness takes priority, and validation expands when meaningful regression risk extends beyond the affected area.
5. `AGENTS.md` contains matching scope/risk-aware validation guidance, consistent with `reviewer.md` and with existing §10/§16.
6. Adaptive routing, conditional Reviewer, context proportionality, agent permissions, role boundaries, and no Tester/QA agent remain unchanged.
7. Scenario verification: docs/config-only → full product suite normally not required; isolated change → affected tests/checks selected; cross-module/high-risk → broader/full regression may be selected when justified.
8. No product code modified; no commit or push.

## Approved Decisions and Constraints

- Validation is scope-, risk-, and change-aware; not a mandatory full-suite ritual.
- Do not artificially restrict validation merely to minimize runtime; correctness over runtime.
- Keep adaptive routing, conditional Reviewer, proportional context, and existing permissions unchanged.
- No separate Tester/QA agent.
- No product code changes.
- No commit or push.

## Relevant Files

- `.opencode/agents/reviewer.md` — insert new `VALIDATION STRATEGY` section between
  `REVIEW FOR` and `OUTPUT`. No other section changes (frontmatter, OPERATING
  RULES, OUTPUT, TASK MEMORY OWNERSHIP unchanged).
- `AGENTS.md` §10 — insert new `### Scope- and Risk-Aware Validation` subsection
  between "If a required validation cannot be run, state that clearly." and
  `### Validation Correction Limit`. No other section changes.
- `.task.md` — task memory only.

## Architecture / Implementation Plan

**Authority split (single home per statement)**
- `AGENTS.md` §10 is authoritative for the project-wide validation principle and
  the general risk-scaled expectations (applies to all agents, including the
  Developer). It does not reference `reviewer.md`.
- `.opencode/agents/reviewer.md` is authoritative for how the Reviewer
  operationalizes validation selection. Its `VALIDATION STRATEGY` section
  references `AGENTS.md` §10 for the principle rather than restating it.
- The two category lists are complementary, not duplicates: §10 states the
  general project-wide expectation; `reviewer.md` states the Reviewer's concrete
  selection procedure. No contradiction is introduced with §15 or §16.
- No change to adaptive routing, conditional Reviewer, §15 Context
  Proportionality, §16 Bounded Context Discovery, permissions, role boundaries,
  or the no-Tester decision.

**Placement rationale (§10 vs §15)**
- Recommended home is §10: it is the existing validation section and the
  guidance governs validation depth, not agent-invocation workflow. §15 governs
  which agents run and how much context they receive; validation depth is a
  distinct concern. Keep the single authority in §10; do not split into §15.

**Edit 1 — `AGENTS.md`, insert after "If a required validation cannot be run,
state that clearly." and before `### Validation Correction Limit` (exact):**

```markdown
### Scope- and Risk-Aware Validation

Validation must be proportional to the change, not a fixed full-suite ritual.

Before running validation, identify the changed files or components, the
behavior those changes could affect, and the existing checks that cover that
behavior; then select the smallest validation set that gives reasonable
confidence in the change.

"Minimum sufficient validation" does not mean "run fewer tests regardless of
risk." Correctness takes priority over runtime. Expand validation whenever
meaningful regression risk extends beyond the directly affected area.

Typical validation depth by change:

- documentation-only or configuration-only: no product test suite is normally
  required; verify the edited document or configuration is well-formed and
  consistent with the surrounding content;
- UI-only: relevant UI/component tests and, where behavior or workflow is
  affected, manual verification of the affected flow;
- isolated backend change: the tests and type checks covering the changed
  module and its direct contracts;
- AI/provider change: the tests covering the provider integration plus
  validation of prompt, request, response, error, and fallback handling;
- database or schema change: migration and persistence tests, including
  upgrade/rollback or data-compatibility checks where applicable;
- cross-module change: tests for every affected module and their integration
  boundaries;
- major architectural or otherwise high-risk change: the broadest relevant
  regression set, including the full suite when justified.

Running the full suite is permitted when the change justifies it; it is not
the default.
```

**Edit 2 — `.opencode/agents/reviewer.md`, insert after the `REVIEW FOR` list
(after "- Missing validation for changed behavior.") and before `OUTPUT`
(exact):**

```markdown
VALIDATION STRATEGY

The general validation principle is defined in AGENTS.md §10 (Scope- and
Risk-Aware Validation). Apply it as follows.

Before running any validation, identify:

1. The files and components that changed.
2. The behavior those changes could affect.
3. The existing tests and checks that cover that behavior.
4. The smallest validation set that gives reasonable confidence in the change.

Run that selected validation, then state why it was sufficient. Do not default
to the full test suite, and do not run validation the change does not justify.

Select validation depth from the change's risk:

- documentation-only or configuration-only: no product test suite; confirm the
  changed document or configuration is well-formed and consistent;
- UI-only: relevant UI/component tests, plus manual verification when behavior
  or workflow is affected;
- isolated backend change: tests and type checks for the changed module and its
  direct contracts;
- AI/provider change: provider-integration tests plus prompt, request,
  response, error, and fallback handling;
- database or schema change: migration and persistence tests, including
  upgrade/rollback or data-compatibility checks where applicable;
- cross-module change: tests for every affected module and their integration
  boundaries;
- major architectural or otherwise high-risk change: the broadest relevant
  regression set, including the full suite when justified.

"Minimum sufficient validation" does not mean "run fewer tests regardless of
risk." Correctness takes priority over runtime. Expand the validation set when
meaningful regression risk extends beyond the directly affected area.

The full test suite may be run when justified; it is not the default.
```

**Scenario verification (maps to acceptance criterion 7)**
- docs/config-only: both texts state no product suite is normally required and
  direct the verifier to confirm the document/configuration is well-formed.
- isolated change: both texts direct selection of the tests/checks covering the
  changed module and its direct contracts.
- cross-module/high-risk: both texts direct the broadest relevant regression set
  and permit the full suite when justified.
- no-misread check: both texts contain the explicit clause that minimum
  sufficient validation does not mean fewer tests regardless of risk and that
  validation expands when risk extends beyond the affected area.

## Implementation Status

Implemented. Edit 1 inserted verbatim into `AGENTS.md` §10 between the
"cannot be run" sentence and `### Validation Correction Limit`. Edit 2 inserted
verbatim into `.opencode/agents/reviewer.md` between `REVIEW FOR` and `OUTPUT`.
No other sections, frontmatter, or product files changed. `npm run lint` ran
with 0 errors (13 pre-existing warnings). Not committed or pushed.

## Review Findings

Review passed — no actionable defects found.

Verified:
- `AGENTS.md` §10 `### Scope- and Risk-Aware Validation` sits after the
  "cannot be run" sentence (line 235) and before `### Validation Correction
  Limit` (line 271); content matches the approved plan verbatim and includes the
  seven risk categories, the correctness-over-runtime/escalation clause, and the
  "full suite is not the default" statement.
- `.opencode/agents/reviewer.md` `VALIDATION STRATEGY` sits between `REVIEW FOR`
  and `OUTPUT`; includes the four pre-test decision questions, the run-and-explain
  requirement, the seven risk categories, the correctness-over-runtime/escalation
  clause, and the "full suite not the default" statement; references `AGENTS.md`
  §10 rather than restating the principle.
- No contradiction between §10 and `reviewer.md` category lists (complementary);
  neither conflicts with §15 Context Proportionality or §16 Bounded Context
  Discovery. Frontmatter, OPERATING RULES, REVIEW FOR, OUTPUT, and TASK MEMORY
  OWNERSHIP unchanged.
- Scenario mapping holds for docs/config-only, isolated change, and
  cross-module/high-risk change.
- Routing, conditional Reviewer, role boundaries, and permissions unchanged; no
  Tester/QA agent present (agents directory contains only architect, developer,
  orchestrator, reviewer).

## Validation

Applied the new scope/risk-aware strategy: this is a documentation/configuration-only
change, so no product test suite was run. Validation performed was static and
targeted: read the changed sections in `AGENTS.md` (lines 235-271) and
`.opencode/agents/reviewer.md` (lines 39-102), confirmed exact placement and
verbatim match against the approved plan, checked the category lists and
cross-references for consistency with §15/§16, and listed `.opencode/agents/` to
confirm no Tester/QA agent. This is sufficient because the change affects no
runtime behavior, API, schema, or provider integration; the only risk is textual
well-formedness and cross-document consistency, which these checks cover. No
shell/git commands were executed (disallowed under reviewer operating
constraints); scope was verified by direct inspection of the changed files and
agent directory.