# Task State

## Objective

Convert the multi-agent workflow from a fixed waterfall (Orchestrator → Architect → Developer → Reviewer) into Orchestrator-driven dynamic routing that selects the minimum sufficient agent workflow per task. Add an explicit context-proportionality principle and update shell permissions to gate both commit and push. No product code changes.

## Acceptance Criteria

1. Orchestrator classifies each task and selects the minimum sufficient workflow: Developer alone; Developer → Reviewer when review adds value; Architect → Developer → Reviewer for architectural/high-risk work; minimum appropriate agents for documentation/configuration-only work.
2. Architect and Reviewer are not invoked automatically for every task; escalation depends on complexity, architectural impact, risk, and change scope.
3. Orchestrator acts as router/coordinator, not a mandatory pipeline stage; the architect/developer/reviewer boundaries are preserved.
4. Context principle: agent invocation and context transfer must be proportional to the task; do not pass the Orchestrator's entire accumulated context; allow targeted/section/range-based discovery; avoid redundant discovery and repeated validation.
5. All four agents: `bash` allow, with `git commit*` ask and `git push*` ask. Existing edit permissions unchanged.
6. `AGENTS.md` updated to describe workflow routing and context proportionality, consistent with the Orchestrator definition.
7. Classification scenarios verified: simple documentation/config change does not invoke Architect; normal implementation normally goes Developer → Reviewer; architectural change invokes Architect → Developer → Reviewer.
8. Resulting configuration is internally consistent (no residual waterfall/mandatory-pipeline statements).
9. No product code modified; no commit or push.

## Approved Decisions and Constraints

- No rigid decision matrix; the Orchestrator exercises judgment.
- Preserve agent boundaries: Orchestrator coordination/routing/`.task.md`; Architect architecture/reasoning/planning; Developer implementation; Reviewer independent review/verification.
- Shell model: all agents `bash: allow`; `git commit*: ask`; `git push*: ask`.
- Keep existing edit permissions.
- Do not modify product code.
- No commit or push.

## Relevant Files

- `.opencode/agents/orchestrator.md` — §2 (rewrite to Workflow Routing), §§5–7 (conditional framing), §3 (context cross-reference), §14 (conditional-invocation clause). Frontmatter bash block.
- `.opencode/agents/architect.md` — frontmatter bash block only.
- `.opencode/agents/developer.md` — frontmatter bash block only.
- `.opencode/agents/reviewer.md` — frontmatter bash block only.
- `AGENTS.md` — §15 add `### Workflow Selection` and `### Context Proportionality`.
- `.task.md` — this plan only (no other section edits).

## Architecture / Implementation Plan

No product code. Documentation/agent-configuration only. Execute the changes below exactly.

### 1. `orchestrator.md` §2 — replace title and body

Replace `## 2. Workflow` and its entire body (lines 41–62) with:

```markdown
## 2. Workflow Routing

The Orchestrator is a router and coordinator, not a mandatory pipeline stage.
For each task, classify the task and select the minimum sufficient set of
subagents. Do not invoke a subagent whose role does not add value for the task.

### 2.1 Classification

Classify each task before invoking a subagent using these illustrative signals
(not a rigid matrix):

- Architectural impact: does the task change architecture, contracts, storage,
  security behavior, dependencies, or multi-module behavior?
- Risk: could a defect cause data loss, security exposure, or break existing
  behavior?
- Change scope: how many files/subsystems are affected, and how much design
  judgment is required?
- Reversibility: is the change easy to inspect and validate locally?
- Review value: is independent verification materially useful, or is the change
  low-risk documentation/configuration?

### 2.2 Minimum Sufficient Workflow

Select the smallest workflow that covers the classified risk. Illustrative
mappings:

- Documentation- or configuration-only change with no architectural impact:
  Developer alone.
- Normal implementation change: Developer → Reviewer.
- Architectural, high-risk, cross-module, or contract-changing work:
  Architect → Developer → Reviewer.

These are guidance, not a checklist. The Orchestrator exercises judgment and
may escalate or de-escalate as evidence emerges.

### 2.3 Escalation

Invoke Architect only when the task requires design decisions, architectural
impact, or a non-trivial implementation plan.

Invoke Reviewer only when independent verification adds value for the
classified risk. Reviewer is not invoked automatically for low-risk
documentation/configuration-only work.

Architect and Reviewer are conditional, not mandatory pipeline stages.

### 2.4 Flow

1. Establish the task objective and acceptance criteria.
2. Classify the task and select the minimum sufficient workflow.
3. Record the objective, acceptance criteria, and selected workflow in .task.md.
4. Invoke only the selected subagents, recording each result in .task.md.
5. Apply the Correction Cycle (§8) when actionable defects remain.
6. Stop when acceptance criteria are satisfied, applicable review has no
   actionable defects, and required validation has passed.

Provide only the context each subagent requires (§3). Do not pass the
Orchestrator's full accumulated context.
```

This removes the fixed 12-step sequence and the "Do not skip Architect or
Reviewer" sentence. Correction-cycle behavior now lives only in §8.

### 2. `orchestrator.md` §§5–7 — conditional framing

- §5 Architect, replace first line `Invoke Architect when analysis or
  implementation planning is required.` with:
  `Invoke Architect only when the selected workflow requires architecture, design, or implementation planning (§2). Architect is not a mandatory pipeline stage.`
  Keep the remainder of §5 unchanged.
- §6 Developer, replace first line `Invoke Developer only after the
  implementation plan is established.` with:
  `Invoke Developer for any task that requires implementation. When the selected workflow includes Architect, invoke Developer only after the implementation plan is established; otherwise invoke Developer directly with the objective, acceptance criteria, and relevant files.`
  Keep the remainder of §6 unchanged.
- §7 Reviewer, replace first line `Invoke Reviewer after implementation and
  required validation are available.` with:
  `Invoke Reviewer only when the selected workflow includes independent review (§2), after implementation and required validation are available.`
  Keep the remainder of §7 unchanged.

### 3. `orchestrator.md` §3 Task Memory — context cross-reference

After the existing `- Do not pass large tool outputs.` bullet and before
`- Do not require the subagent to rediscover...`, insert:

`- Transfer context proportionally to the task (AGENTS.md §15, "Context Proportionality"); do not pass the Orchestrator's entire accumulated context.`

No other §3 change (avoids duplicating the authoritative AGENTS.md principle).

### 4. `orchestrator.md` §14 — conditional-invocation clause

After `Do not invoke other subagents.` insert:

`Invoking a permitted subagent is conditional on the selected workflow (§2). This list defines which subagents may be invoked; it does not require any of them to be invoked.`

Keep remaining §14 text unchanged.

### 5. `AGENTS.md` §15 — add two subsections

Insert after the `### Task Boundary` subsection and before
`### Source-Code and Documentation Discovery`:

```markdown
### Workflow Selection

The Orchestrator classifies each task and selects the minimum sufficient agent
workflow rather than running a fixed pipeline. The permitted agents are the
architect, developer, and reviewer; invoking any of them is conditional.

Classify a task by architectural impact, risk, change scope, reversibility, and
the value of independent verification, then select the smallest workflow that
covers that risk. For example:

- documentation or configuration change with no architectural impact:
  Developer alone;
- normal implementation change: Developer → Reviewer;
- architectural, high-risk, or cross-module change:
  Architect → Developer → Reviewer.

These mappings are illustrative, not a rigid matrix. Escalate or de-escalate as
evidence emerges. The architect and reviewer are invoked when their role adds
value, not automatically. Agent role boundaries are unchanged.

### Context Proportionality

Agent invocation and context transfer must be proportional to the task.

- Invoke only the agents whose role adds value for the classified task.
- Pass each agent only the context required for its role; do not pass the
  Orchestrator's entire accumulated context or conversation history.
- Prefer targeted, section-level, or range-based reading over whole-file or
  whole-repository reads.
- Do not require an agent to rediscover information already established in
  .task.md.
- Do not repeat discovery or validation already performed and recorded in
  .task.md.

This subsection is the authoritative statement of context proportionality and is
consistent with `## 16. Bounded Context Discovery`.
```

The `Context Proportionality` subsection is the single authoritative wording;
`orchestrator.md` §3 only cross-references it.

### 6. Bash frontmatter — all four agents

In `orchestrator.md`, `architect.md`, `developer.md`, `reviewer.md`, replace each
`bash` block with:

```yaml
  bash:
    "*": allow
    "git commit*": ask
    "git push*": ask
```

Catch-all first, specific overrides after; `commit` before `push`. Edit and
`task` permissions unchanged.

### 7. Consistency verification (after edits)

Confirm no residual mandatory-pipeline statements remain:

- `orchestrator.md`: no `use this sequence`; no `Do not skip Architect or
  Reviewer`; no wording that Architect/Reviewer run for every implementation
  task. §§5–7 first lines conditional.
- `AGENTS.md` §15: `### Workflow Selection` and `### Context Proportionality`
  present, adjacent, before `### Source-Code and Documentation Discovery`; §16
  unchanged.
- Both files describe the same routing model (same three illustrative mappings)
  and neither contradicts the other.
- All four `bash` blocks identical with the three keys.

### 8. Classification test walkthroughs

- Simple documentation/config change: classified no architectural impact,
  low risk, low review value → Developer alone; no Architect, no Reviewer.
- Normal implementation change: classified moderate risk where independent
  verification adds value → Developer → Reviewer; no Architect.
- Architectural change: classified architectural/high-risk/cross-module →
  Architect → Developer → Reviewer.

Each matches the §2.2 guidance and the AGENTS.md §15 Workflow Selection
mappings.

## Implementation Status

Implemented (steps 1–6 applied verbatim, plus Reviewer correction). Modified: `.opencode/agents/orchestrator.md` (§2 rewritten to Workflow Routing; §§5–7 first lines conditional; §3 context cross-reference; §14 conditional-invocation clause; §13 completion bullet changed to `- applicable review has no actionable defects;`; bash block), `.opencode/agents/architect.md`, `.opencode/agents/developer.md`, `.opencode/agents/reviewer.md` (bash block), and `AGENTS.md` §15 (added `### Workflow Selection` and `### Context Proportionality`). No product code changed. `npm run lint`: 0 errors (13 pre-existing source warnings). Residual check on `orchestrator.md`: no wording requiring Architect or Reviewer for every task; remaining matches are negations ("not a mandatory pipeline stage"). No commit or push.

## Review Findings

Review passed — no actionable defects found. The prior §13 Completion finding is resolved: line 334 now reads `- applicable review has no actionable defects;`, consistent with §2.4 step 6 and dynamic routing.

## Validation

- Re-review after the §13 correction. Reviewed: `.opencode/agents/orchestrator.md`, `.opencode/agents/architect.md`, `.opencode/agents/developer.md`, `.opencode/agents/reviewer.md`, `AGENTS.md` §15–§16, `.task.md`.
- §13 Completion line 334 reads `- applicable review has no actionable defects;`. No unconditional Reviewer/Architect requirement remains in `orchestrator.md`; remaining "mandatory" mentions are explicit negations ("not a mandatory pipeline stage").
- Previously-passing items re-confirmed: §2.1–§2.4 routing (classification, minimum sufficient workflow, escalation, flow) present and coherent; §§5–7 first lines conditional; §3 cross-reference to `AGENTS.md` §15 Context Proportionality; §14 conditional-invocation clause; `AGENTS.md` §15 `### Workflow Selection` and `### Context Proportionality` adjacent and before `### Source-Code and Documentation Discovery`, consistent with §16; three mappings match `orchestrator.md` §2.2.
- All four agents' `bash` blocks are exactly `"*": allow`, `"git commit*": ask`, `"git push*": ask`; `edit`/`task`/`webfetch`/`websearch` boundaries preserved; no `model:` key in any frontmatter.
- Classification scenarios route as documented: (a) documentation/config change → Developer alone; (b) normal implementation change → Developer → Reviewer; (c) architectural/high-risk change → Architect → Developer → Reviewer.
- Scope independently reproduced by the Orchestrator via shell: `git status --short` shows only ` M .task.md`, ` M AGENTS.md`, `?? .opencode/`; no product code modified; no `opencode.json`; no `model:` entries. `npm run lint` 0 errors (13 pre-existing warnings); `npx tsx --test tests/*.test.ts` 49 tests, 0 fail.